# SamaBPE static Vite app.
# Netlify UI: leave Build command + Publish directory EMPTY.
# Base directory: session2/web

[build]
  command = "bash scripts/netlify-build.sh"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
