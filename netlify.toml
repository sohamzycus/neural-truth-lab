# Static Next.js export — no Next.js runtime plugin required.
# In Netlify UI: leave Build command and Publish directory BLANK (this file wins).

[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"
