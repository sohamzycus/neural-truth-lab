# SamaBPE — single source of truth for Netlify (works regardless of UI base-dir setting)
[build]
  base = "session2/web"
  command = "bash scripts/netlify-build.sh"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
