# Static Next.js export. Netlify UI: leave Build command + Publish directory EMPTY.

[build]
  command = "bash scripts/netlify-build.sh"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"
  # ponytail: noop Netlify's auto npm install; our script runs npm ci instead
  NPM_FLAGS = "--version"
