name: Deploy SamaBPE to Netlify (session2 only)

# Site: https://sama-bpe-tokenizer.netlify.app
# Session1 (llmlab) deploys via Netlify Git with base directory session1 — see DEPLOYMENT.md

on:
  push:
    branches: [main]
    paths:
      - 'session2/web/**'
      - 'session2/results/**'
      - 'session2/python/**'
      - 'session2/scripts/**'
      - '.github/workflows/netlify-deploy.yml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: session2/web
    steps:
      - uses: actions/checkout@v4

      - name: Verify prebuilt dist
        run: test -f dist/index.html && test -d dist/data/results

      - name: Deploy to Netlify
        run: |
          cd dist
          zip -r ../deploy.zip .
          curl -fsS -H "Authorization: Bearer $NETLIFY_AUTH_TOKEN" \
               -H "Content-Type: application/zip" \
               --data-binary @../deploy.zip \
               "https://api.netlify.com/api/v1/sites/$NETLIFY_SITE_ID/deploys"
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
