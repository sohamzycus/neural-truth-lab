# erav5

Monorepo with two deployable sessions:

| Session | App | Netlify base directory |
|---------|-----|----------------------|
| `session1/` | Neural Truth Lab (Next.js) | `session1` |
| `session2/` | SamaBPE Fair Tokenizer Lab (Vite) | `session2/web` |

Both connect to the same GitHub repo: `sohamzycus/neural-truth-lab`.

See `session1/docs/DEPLOYMENT.md` and `session2/docs/DEPLOYMENT.md`.
